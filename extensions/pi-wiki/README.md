# pi-wiki

Local wiki tools for pi.

## Features

- `/wiki <url>` — Scrape docs into `~/.pi/wiki/<tool>/`
- `wiki_search` tool — Search local wiki entries by relevance

## Storage

Wiki entries are stored in `~/.pi/wiki/`.
